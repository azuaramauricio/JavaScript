const producto = {
    nombre: "monitor de 20 pulgadas",
    precio: 200,
    disponible: true
}

const {nombre, precio, disponible} = producto;


console.log(nombre);
console.log(precio);
console.log(disponible);